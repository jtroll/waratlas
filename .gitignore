# raw source datasets (derive-and-cite only; never republish raw files)
datasets/**
!datasets/**/
!datasets/README.md

# dependencies
/node_modules
/.pnp
.pnp.js

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# local env files
.env*.local

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

# ─────────────────────────────────────────────────────────────
# Dev / data-build artifacts at the project root.
# These are SOURCES used to build public/empires.json + public/cities.json,
# not runtime files. Excluded from the repo to keep clone + deploy fast.
# Keep them locally if you'll re-run the data build; copy off-machine if
# you want them under version control elsewhere.
# ─────────────────────────────────────────────────────────────
/world_*.geojson
/world_*.json
/places.geojson
/fix_*.json
/outputs/
/backups/

# Old standalone 